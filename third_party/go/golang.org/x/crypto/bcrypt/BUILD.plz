subinclude("///pleasegomod//:build_defs")

fileexport(
    name = "bcrypt",
    srcs = [
        "bcrypt/base64.go",
        "bcrypt/bcrypt.go",
    ],
    tag = "go_source",
    deps = ["//third_party/go/golang.org/x/crypto:_crypto#download"],
)

go_library(
    name = "bcrypt",
    srcs = [":_bcrypt#go_source"],
    cover = False,
    import_path = "golang.org/x/crypto/bcrypt",
    visibility = ["PUBLIC"],
    deps = ["//third_party/go/golang.org/x/crypto/blowfish"],
)
