subinclude("///pleasegomod//:build_defs")

fileexport(
    name = "semaphore",
    srcs = ["semaphore/semaphore.go"],
    tag = "go_source",
    deps = ["//third_party/go/golang.org/x/sync:_sync#download"],
)

go_library(
    name = "semaphore",
    srcs = [":_semaphore#go_source"],
    cover = False,
    import_path = "golang.org/x/sync/semaphore",
    visibility = ["PUBLIC"],
    deps = [],
)
