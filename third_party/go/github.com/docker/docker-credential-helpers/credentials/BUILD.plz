subinclude("///pleasegomod//:build_defs")

fileexport(
    name = "credentials",
    srcs = [
        "credentials/credentials.go",
        "credentials/error.go",
        "credentials/helper.go",
        "credentials/version.go",
    ],
    tag = "go_source",
    deps = ["//third_party/go/github.com/docker/docker-credential-helpers:_docker-credential-helpers#download"],
)

go_library(
    name = "credentials",
    srcs = [":_credentials#go_source"],
    cover = False,
    import_path = "github.com/docker/docker-credential-helpers/credentials",
    visibility = ["PUBLIC"],
    deps = [],
)
